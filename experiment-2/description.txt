
## 🔷 PROJECT OVERVIEW

This **Banking UI** project is a clean and minimal front-end design built using **HTML** and **CSS**, meant to represent the look of a simple **banking interface**. It visually demonstrates:

1. **Balance display** area for showing the account amount.
2. **Amount input field** for entering deposits or withdrawals.
3. **Action buttons** for Deposit and Withdraw.

**Note:** This is a purely **static front-end** project. No JavaScript or functional logic is implemented — the UI is **visual only**.

---

## 🧱 HTML STRUCTURE & SEMANTICS

* The UI is placed inside a central container `.banking-ui`.
* Semantic and accessible HTML elements are used for clarity.
* **Key elements:**

  * `#balance` → Displays balance (static text).
  * `#amount` → Input field for number entry.
  * Buttons:

    ```html
    <button class="deposit-btn">Deposit</button>
    <button class="withdraw-btn">Withdraw</button>
    ```

---

## 🎨 CSS STYLING BREAKDOWN

### 1. **body**

* **Purpose:** Centers the banking card.
* **CSS:**

  ```css
  display: flex;
  justify-content: center;
  align-items: center;
  min-height: 100vh;
  background-color: #f0f4f8;
  ```

### 2. **.banking-ui (Container)**

* **Purpose:** Main card layout.
* **CSS:**

  ```css
  background-color: #ffffff;
  padding: 30px;
  border-radius: 10px;
  box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
  width: 300px;
  ```

### 3. **#balance (Balance Display)**

* **Purpose:** Shows static account balance.
* **CSS:**

  ```css
  background-color: #e0f7e9;
  color: #2e7d32;
  font-weight: bold;
  padding: 15px;
  margin-bottom: 20px;
  text-align: center;
  border-radius: 5px;
  ```

### 4. **#amount (Input Field)**

* **Purpose:** Input for transaction amount (no functionality).
* **CSS:**

  ```css
  width: 100%;
  padding: 10px;
  margin-bottom: 15px;
  border-radius: 5px;
  border: 1px solid #ccc;
  font-size: 16px;
  ```

### 5. **.deposit-btn / .withdraw-btn (Buttons)**

* **Purpose:** Represent actions visually.
* **CSS Common:**

  ```css
  width: 100%;
  padding: 12px;
  margin-top: 10px;
  font-size: 18px;
  border-radius: 5px;
  border: none;
  cursor: pointer;
  ```
* **Deposit Button:**

  ```css
  background-color: #4caf50;
  color: white;
  ```
* **Withdraw Button:**

  ```css
  background-color: #f44336;
  color: white;
  ```

---

## ✅ LEARNING OUTCOMES

1. How to create a **structured UI** with HTML + CSS only.
2. Understanding **flexbox centering** for responsive layouts.
3. Using **color & styling cues** for differentiating actions.


